- hosts: messaging
  tasks:
    - name: "Ping all containers"
      ping:


- hosts: brokers
  tasks:
    - name: "Ping broker containers"
      ping:

- hosts: clients
  tasks:
    - name: "Ping client containers"
      ping:


- hosts: routers
  tasks:
    - name: "Ping router containers"
      ping:
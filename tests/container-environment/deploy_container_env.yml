- name: Bring up docker containers environment
  hosts: localhost
  gather_facts: false
  roles:
    - role: provision_docker
      provision_docker_privileged: true,
      provision_docker_inventory_group: "{{ groups['messaging'] }}"
      provision_docker_use_docker_connection: true
